@page
@model Reservation.Pages.ParkingSpaces.DetailsModel

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>ParkingSpace</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.ParkingSpace.TypeOfParkingSpace)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.ParkingSpace.TypeOfParkingSpace)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.ParkingSpace.Price)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.ParkingSpace.Price)
        </dd>
    </dl>
</div>
<div>
    <a asp-page="./Edit" asp-route-id="@Model.ParkingSpace.Id">Edit</a> |
    <a asp-page="./Index">Back to List</a>
</div>
